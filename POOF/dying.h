#pragma once

// typedef std::vector < std::pair < std::string, int>> pairlist_t;
// typedef std::string text_t;
//	using std::cout;

//	number_t students = 6 - (5 + 4) * 3 / 2;

	// addition + subtraction
//	students++;
// 	students = students - 1;
//	students -= 1;
//	students--;

	// multiplication
//	students = students * 200;
//	students *= 200;

	// dividing 
//	students /= 6;

	// dividing into groups ?? 

// using text_t = std::string;
// using number_t = int;